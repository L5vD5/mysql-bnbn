type Query {
  readProduct: [Product!]!
  FamilyCategory:[FamilyCategory!]!
}

type Query {
  selectProduct(id: Int, productname: String!): Product!
}

type Query {
  searchProduct(productname: String!): Product!
}
